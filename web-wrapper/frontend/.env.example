# Backend API base URL. Defaults to http://localhost:8000 when unset.
VITE_API_URL=

# Google Identity (ID token) sign-in. Same value as the backend's
# GOOGLE_OAUTH_CLIENT_ID. When unset, the "Sign in with Google" button is hidden.
VITE_GOOGLE_CLIENT_ID=
